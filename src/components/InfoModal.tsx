import React from 'react';

/**
 * InfoModal component - currently not used
 * This component can be used to display upgrade information
 * For now, InfoPanel handles all upgrade information display
 */
const InfoModal: React.FC = () => {
  return null;
};

export default InfoModal; 